Ask the agent from Day 3: "Analise completa de satisfacao por regiao com impacto financeiro e plano de acao."
Watch it struggle — one agent trying to write SQL, search vectors, AND synthesize a report.

Now think about a company: the analyst doesn't write the report. The researcher doesn't run SQL.
Each specialist does ONE thing well.

That's multi-agent: instead of 1 overloaded agent, you build a TEAM.
Today we build 3 specialists: AnalystAgent (SQL), ResearchAgent (vectors), ReporterAgent (synthesis).
