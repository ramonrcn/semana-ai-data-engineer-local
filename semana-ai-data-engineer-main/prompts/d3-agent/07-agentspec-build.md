Use AgentSpec to build the ShopAgent code:

/agentspec:build SHOPAGENT

Watch the specialist agents being delegated automatically.
Review the generated code:
1. Are the tools properly defined with clear descriptions for routing?
2. Does the agent use the ReAct pattern with Claude?
3. Is the Chainlit integration included?
4. Are there tests?

Check the build report. We'll refine the code manually in the next prompts.
