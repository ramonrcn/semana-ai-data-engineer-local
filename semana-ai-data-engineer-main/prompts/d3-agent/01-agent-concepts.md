Explain the difference between a Tool, an Agent, and a Workflow using ShopAgent
as example for each:

1. Tool: "I call supabase_execute_sql, it runs, returns data" (screwdriver)
2. Agent: "I ask a question, the agent decides WHICH tool to use" (robot with tools)
3. Workflow: "I define the flow, multiple agents execute" (robot factory)

Yesterday YOU decided when to use Qdrant vs Postgres. Today the AGENT decides.
What changes in the architecture when we go from tool to agent?
