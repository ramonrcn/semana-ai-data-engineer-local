name: Local Config
version: 1.0.0
schema: v1

models:
  - name: Qwen Coder
    provider: ollama
    model: qwen2.5-coder:7b
    apiBase: http://localhost:11435

mcpServers:
  - name: shopagent-mcp
    command: python
    args:
      - -m
      - mcp_server.server

contextProviders:
  - name: codebase

chatOptions:
  baseSystemMessage: |
    You must always call execute_task. Never read files. Never generate code.